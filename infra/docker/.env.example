# Cloudflare Tunnel token (recommended secure mode)
# Create in Cloudflare Zero Trust > Networks > Tunnels > <your tunnel> > Run connector
CLOUDFLARED_TUNNEL_TOKEN=REPLACE_WITH_REAL_TOKEN

# Optional
TZ=Asia/Tokyo
